spring:
  application:
    name: trans-trader

server:
  port: 8080

# 定时任务配置
spring:
  task:
    scheduling:
      pool:
        size: 10

# OKEx API 配置
okex:
  api:
    key: your-api-key
    secret: your-api-secret
    passphrase: your-api-passphrase
    base-url: https://www.okex.com
