<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="log_home" value="/var/log/okex-sdk" />

    <!-- 控制台输出 -->
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{20} - [%method,%line] - %msg%n</pattern>-->
            <pattern>%d{HH:mm:ss} | %-5level %logger{20} | [%method,%line] | %msg%n</pattern>
        </encoder>
    </appender>

    <!-- logStash -->
<!--    <appender name="logStash" class="net.logstash.logback.appender.LogstashTcpSocketAppender">-->
<!--        <destination>127.0.0.1:8801</destination>-->
<!--        <encoder class="net.logstash.logback.encoder.LogstashEncoder" />-->
<!--        <keepAliveDuration>15 minutes</keepAliveDuration>-->
<!--    </appender>-->

    <!-- 系统日志输出 -->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log_home}/daily.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 按天回滚 daily -->
            <fileNamePattern>${log_home}/daily.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 日志最大的历史 30天 -->
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss} | %-5level %logger{20} | [%method,%line] | %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 显示形成的sql、使用的参数、结果集 -->
    <!--
        <logger name="java.sql" level="debug" />
        <logger name="org.springframework.jdbc" level="debug" />
    -->

    <logger name="com.okex.open.api" level="debug" />

    <root level="debug">
        <appender-ref ref="stdout" />
<!--        <appender-ref ref="logStash" />-->
        <appender-ref ref="file" />
    </root>

</configuration>
